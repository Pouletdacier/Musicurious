<div class="row">
  <% @instruments.each do |instrument| %>
    <div class="card" style="width: 18rem;">
      <%= cl_image_tag instrument.photos.first.key, crop: :lfill, resize: :lpad %>

      <div class="card-body">
        <h4><strong><%= instrument.name  %></strong></h4><br>
        <p class="card-text">
          <em><%= instrument.family%></em> <br>
          <%= instrument.origins  %> <br>
          <strong><%= instrument.geographical_region  %></strong>
        </p>
      </div>
    </div>
  <%end%>
</div>
